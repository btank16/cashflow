import { StyleSheet } from 'react-native';

const CommonStyles = StyleSheet.create({
  logo: {
    height: 45,
    aspectRatio: 'auto',
    resizeMode: 'contain',
    maxWidth: '100%',
  }
});

export default CommonStyles;